export enum WordGender {
    masculine,
    feminine,
    neuter
}

export enum WordType {
    noun,
    adjective
}

export enum WordCase {
    nominative,
    accusative,
    dative,
    genitive,
    instrumental,
    prepositional
}

export enum WordQuantity {
    singular,
    plural
}

export enum Animate {
    inanimate,
    animate
}
